image: continuumio/miniconda3:latest

tests:
  stage: test
  script:
    - >
      for network in gnn lin; do
          for agent in ppo dppo ippo; do
            echo "Hi agent $agent"
            echo "Hi network $network"
      done
